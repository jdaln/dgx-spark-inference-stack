services:
  waker:
    environment:
      EXTERNAL_GPU_POLICY: ${EXTERNAL_GPU_POLICY:-observe}
      EXTERNAL_GPU_CONTAINER_NAMES: ${EXTERNAL_GPU_CONTAINER_NAMES:-comfyui}
      WORKLOADS_CONFIG_PATH: ${WORKLOADS_CONFIG_PATH:-}
      EXTERNAL_WORKLOAD_PROBE_TIMEOUT_MS: ${EXTERNAL_WORKLOAD_PROBE_TIMEOUT_MS:-3000}
      EXTERNAL_BUSY_RETRY_AFTER_SECONDS: ${EXTERNAL_BUSY_RETRY_AFTER_SECONDS:-30}
